Example FederationStarbase:

Warning: This Example includes 11 Docks in the FederationStarbase
	 so if you have a low end pc it could be that your pc will load some time
	 when spawning the new ships.
	 The ships will only leave the dock
	 if there are more enemies than friendlies on the map;
	 This plugin is the hand written oreginal file so it is not that clearly laid out
	 but hey it works.